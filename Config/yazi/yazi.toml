[opener]
edit = [
  { run = 'nvim "$@"', block = true, for = "unix" },
]

image = [
  { run = 'imv "$@"', orphan = true, for = "unix" },
]

[open]
prepend_rules = [
  { mime = "text/*", use = "edit" },
  { mime = "image/*", use = "image" },
]
