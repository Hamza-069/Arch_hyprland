ssh-keygen -t ed25519 -C "mhamza111315@gmail.com"
#"Enter" twice


ssh-agent -c > ~/.ssh-agent.fish
source ~/.ssh-agent.fish
ssh-add ~/.ssh/id_ed25519


cat ~/.ssh/id_ed25519.pub
#copy and past in the github

#this to just to check
ssh -T git@github.com

git config --global user.name "Hamza Aslam"
git config --global user.email "mhamza111315@gmail.com"

git remote set-url origin git@github.com:Hamza-069/Arch_hyprland_backup.git

